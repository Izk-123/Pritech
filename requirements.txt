# Core Django
django>=5.2.7
django-htmx>=1.17.0
django-quill>=0.1.0
pillow>=10.0.0

# Admin theme
django-unfold>=0.5.0

# Authentication & social login
django-allauth>=0.63.0
django-otp>=1.3.0
qrcode>=7.4.2
pillow  # already above

# Background tasks
celery>=5.3.0
redis>=5.0.0
django-celery-results>=2.5.0

# PDF generation & sanitisation
weasyprint>=61.2
bleach>=6.1.0

# Audit logging
django-simple-history>=3.5.0

# Cache & rate limiting (database backend works out of box)
# No extra package required for DB cache

# Production database
psycopg2-binary>=2.9.9   # PostgreSQL – optional but recommended

# Environment variables
python-dotenv>=1.0.0

# File type detection (for attachment validation) – optional, we use mimetypes
# python-magic is not required anymore; we use built-in mimetypes

# Additional utilities
django-cors-headers>=4.3.0   # optional, if you later expose API